package Tools;

import Bitmaps.Bitmap;

class CheckPoint
{
    int x;
    int y;
    Bitmap bitmap;
    CheckPoint(int x, int y, Bitmap bitmap)
    {
        this.x = x;
        this.y = y;
        this.bitmap = bitmap;
    }
}

public class Bucket extends Tool{
    public void onPress(int x, int y, Bitmap bitmap)
    {
        while (true)
        {

        }
    }
}
